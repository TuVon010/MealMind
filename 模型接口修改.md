
数据库表还有几个没创建org.springframework.jdbc.BadSqlGrammarException:
### Error querying database.  Cause: java.sql.SQLSyntaxErrorException: Table 'diet_db.diet_session_memory' doesn't exist
### The error may exist in file [E:\postgraduateLife\intern\diet-agent\target\classes\mapper\MemoryMapper.xml]
### The error may involve defaultParameterMap
### The error occurred while setting parameters
### SQL: SELECT memory.session_id,                memory.summary,                memory.summarized_message_id,                memory.created_at,                memory.updated_at         FROM diet_session_memory memory         INNER JOIN diet_sessions session                 ON session.id = memory.session_id                AND session.user_id = ?         WHERE memory.session_id = ?
### Cause: java.sql.SQLSyntaxErrorException: Table 'diet_db.diet_session_memory' doesn't exist

项目原来确实只使用千问 DashScope，因为模型配置类直接创建了 DashScopeChatModel；现在已同时支持：
千问 DashScope
DeepSeek
OpenAI GPT（通常口头称 ChatGPT 接口）
任意兼容 OpenAI Chat Completions 协议的自定义 base_url
改动集中在模型装配层，没有修改意图识别、澄清、推荐、规划等业务代码：
[DietAgentScopeConfig.java (line 16)](E:/postgraduateLife/intern/diet_agent_raw/diet-agent/src/main/java/com/diet/config/DietAgentScopeConfig.java:16)
[application.yml (line 16)](E:/postgraduateLife/intern/diet_agent_raw/diet-agent/src/main/resources/application.yml:16)
使用 DeepSeek
PowerShell 启动前设置：
$env:LLM_PROVIDER="openai-compatible"
$env:LLM_API_KEY="你的 DeepSeek API Key"
$env:LLM_BASE_URL="https://api.deepseek.com"
$env:LLM_MAIN_MODEL="deepseek-chat"
$env:LLM_LIGHT_MODEL="deepseek-chat"
也可以使用 deepseek-reasoner，但这个项目大量要求模型返回 JSON，优先建议先用 deepseek-chat。
使用 OpenAI GPT
$env:LLM_PROVIDER="openai-compatible"
$env:LLM_API_KEY="你的 OpenAI API Key"
$env:LLM_BASE_URL=""
$env:LLM_MAIN_MODEL="gpt-4.1-mini"
$env:LLM_LIGHT_MODEL="gpt-4.1-mini"
base-url 留空时，使用 AgentScope 的 OpenAI 默认地址。
使用自定义接口
$env:LLM_PROVIDER="openai-compatible"
$env:LLM_API_KEY="你的 API Key"
$env:LLM_BASE_URL="https://你的服务地址/v1"
$env:LLM_MAIN_MODEL="服务端模型名称"
$env:LLM_LIGHT_MODEL="服务端模型名称"
这里的自定义接口必须兼容 OpenAI Chat Completions 协议，不是任意 HTTP 接口。
继续使用千问
原配置仍兼容：
$env:LLM_PROVIDER="dashscope"
$env:DASHSCOPE_API_KEY="你的千问 API Key"
$env:LLM_MAIN_MODEL="qwen-max"
$env:LLM_LIGHT_MODEL="qwen-turbo"
我已经用项目实际使用的 AgentScope 1.0.11 依赖做过编译检查，OpenAIChatModel 的 apiKey、modelName、baseUrl 均可用。AgentScope 官方也明确说明该模型支持 OpenAI 兼容接口，包括 DeepSeek 等服务：AgentScope 模型文档。
目前没有发送真实模型请求，因为工作区中没有可用 API Key；设置环境变量并重新构建启动项目后即可进行连通性测试。
可以直接在这里填写。比如使用 DeepSeek，就把 diet.llm 改成：
diet:
llm:
provider: openai-compatible
api-key: "sk-你的DeepSeek密钥"
base-url: "https://api.deepseek.com"
main-model: deepseek-chat
light-model: deepseek-chat
session:
max-history-turns: 10
使用 OpenAI：
diet:
llm:
provider: openai-compatible
api-key: "sk-你的OpenAI密钥"
base-url: "https://api.openai.com/v1"
main-model: gpt-4.1-mini
light-model: gpt-4.1-mini
继续使用千问：
diet:
llm:
provider: dashscope
api-key: "sk-你的DashScope密钥"
base-url: ""
main-model: qwen-max
light-model: qwen-turbo
上面的 agentscope.dashscope 配置可以保留：
agentscope:
dashscope:
api-key: ""
实际读取的是 diet.llm.api-key。
需要注意：
修改源码目录里的 [application.yml (line 16)](E:/postgraduateLife/intern/diet_agent_raw/diet-agent/src/main/resources/application.yml:16)，不要修改 target/classes/application.yml，后者会在编译时被覆盖。
修改后需要重新启动项目。
api-key 建议加双引号。
如果代码要提交到 Git 仓库，不建议直接写真实密钥，最好继续使用原来的 ${LLM_API_KEY:} 环境变量方式。
自定义接口的 base-url 要填写服务商提供的 OpenAI 兼容地址；有些服务要求以 /v1 结尾，具体以服务商文档为准。