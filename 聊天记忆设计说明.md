# Diet-Agent 分层聊天记忆设计

## 1. 目标

本项目不再把“模型内部 Memory”当作可靠记忆，而是由 Java 和 MySQL 显式管理三层记忆：

1. 短期记忆：最近少量消息，保留当前对话的连续性。
2. 会话摘要：把超出短期窗口的旧消息滚动压缩，避免上下文无限增长。
3. 长期记忆：只保存用户明确表达、跨会话仍可能有效的稳定事实。

这种设计借鉴常见 Agent 的“小工作窗口 + 压缩历史 + 持久化事实”思路，但不宣称复刻 Codex、Claude 等产品的内部实现。

## 2. 一轮请求的数据流

```text
加载 SessionState
  -> 读取最近 6 条旧消息
  -> 将更早消息滚动写入 diet_session_memory
  -> 读取用户长期记忆 diet_user_memory
  -> 当前消息写入 diet_messages
  -> 提取当前消息中的明确稳定事实并保存
  -> 长期偏好转为 Java 白名单槽位
  -> IntentAgent 接收：过滤后的摘要 + 过滤后的短期历史 + 已知槽位 + 当前消息
  -> 当前明确槽位覆盖会话状态，会话状态覆盖长期默认偏好
  -> Search / Rank / Response
```

加载历史发生在当前消息落库之前，因此当前消息不会既出现在 `recentHistory`，又重复出现在“当前这一句”。

## 3. 三层记忆

### 3.1 短期记忆

默认配置：

- 最近 6 条消息，不是 6 轮对话。
- 单条最多 160 字符。
- 整个短期窗口最多 960 字符。

相关代码：

- `SessionService#recentConversationTurns`
- `MemoryContext#promptSafeRecentHistory`

### 3.2 会话摘要

短期窗口之外的旧消息按批读取，并写入 `diet_session_memory`：

- 每批最多处理 50 条。
- 每条进入摘要前最多保留 120 字符。
- 滚动摘要最多 800 字符，超出后保留较新的尾部。
- `summarized_message_id` 记录压缩进度，避免每轮重复扫描全部消息。

当前版本采用确定性的提取式压缩，不会为摘要额外调用一次 LLM，因此延迟和费用比较稳定。它不是语义摘要，特别长的会话仍可能丢失较早细节。

### 3.3 长期记忆

长期记忆保存在 `diet_user_memory`，可跨 session 使用。当前类型包括：

- `PREFERENCE`：明确的稳定口味、菜系和便捷偏好。
- `GOAL`：明确的长期健康目标。
- `ALLERGY`：明确表达的过敏食材。
- `AVOID`：明确表达的长期忌口。

系统只在出现“我喜欢、平时喜欢、我偏好、我长期、我不能吃、我忌口”等明确表达时提取。类似“今天想吃清淡粤菜”只是本轮需求，不会被写成长期偏好。

同一用户、类型、字段和值通过唯一索引去重；再次表达会激活并更新时间。

## 4. 冲突优先级

推荐槽位按以下优先级合并：

```text
当前这句话的明确要求
  > 当前 session 已保存的槽位
  > 跨 session 的长期偏好默认值
```

例如长期记忆是“喜欢粤菜”，本轮用户明确说“今天想吃川菜”，最终使用川菜；本轮没有指定菜系时，才默认使用粤菜。

## 5. 隐私与安全边界

- 普通会话摘要和白名单偏好槽位可以进入当前配置的外部模型上下文。
- 摘要和短期历史在进入模型前，会过滤包含过敏、忌口、疾病、治疗、用药等关键词的历史行。
- `ALLERGY`、`AVOID` 等长期敏感事实不会作为长期记忆自动发送给模型。
- 当前用户正在发送的原文仍属于正常模型调用输入；过滤规则针对历史记忆的自动再次发送。
- 项目目前只有客户端传入的 `X-User-Id`，不是真实鉴权。因此暂不开放长期记忆查询/删除 HTTP 接口，避免伪造用户 ID 读取敏感信息。底层已保留停用记忆能力，接入登录鉴权后再开放。

## 6. 当前限制

1. `meal_item` 当前没有结构化配料字段，因此虽然能够记住过敏和忌口，却不能可靠判断某道菜是否含该食材。不能把“保存了过敏记忆”宣传成“已经实现过敏菜品硬过滤”。
2. 摘要是字符级滚动压缩，不是 LLM 语义总结，也没有向量检索。
3. 长期事实采用确定性规则提取，表达过于隐晦时可能识别不到。
4. 用户撤销偏好还缺少完整的自然语言 `SET/REMOVE/CLEAR` 协议。
5. 服务层具有停用长期记忆的能力，但 HTTP 管理接口要等真实鉴权完成后才能安全开放。

## 7. 数据库升级

已有数据库不要重新执行带 `DROP TABLE` 的全量脚本。只执行：

```sql
USE diet_db;
SOURCE E:/postgraduateLife/intern/diet-agent/src/main/resources/db/memory_upgrade.sql;
```

全新环境可直接执行 `src/main/resources/db/diet_db.sql`。

新增表：

- `diet_session_memory`：会话滚动摘要和压缩进度。
- `diet_user_memory`：跨会话稳定事实。

## 8. 关键配置

配置位于 `application.yml` 的 `diet.memory`：

```yaml
memory:
  enabled: true
  short-term-messages: 6
  message-max-chars: 160
  short-term-max-chars: 960
  summary-batch-size: 50
  summary-max-chars: 800
  summary-message-max-chars: 120
  long-term-limit: 20
  long-term-max-chars: 600
```

记忆表不可用时，`ConversationMemoryService` 会记录警告并降级为最近消息上下文，不让记忆故障直接中断推荐。

## 9. 面试简答

> 项目使用 Java 和 MySQL 实现显式分层记忆。短期层只给模型最近 6 条消息，并设置单条和总字符预算；更早消息增量压缩为滚动摘要；用户明确表达的稳定偏好和目标跨会话保存为长期记忆。合并时当前要求优先于会话状态，会话状态优先于长期默认值。敏感的过敏、忌口长期事实不自动发送给外部模型。目前摘要是确定性提取式压缩，还没有向量检索或真正的语义摘要，这是后续可以继续演进的部分。
